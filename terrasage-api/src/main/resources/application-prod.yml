spring:
  jpa:
    hibernate:
      ddl-auto: validate   # 운영: 스키마 자동 변경 금지, 불일치 시 시작 실패
    show-sql: false
  sql:
    init:
      mode: never          # 운영: data.sql 실행 안 함

logging:
  level:
    com.terrasage: INFO    # 운영: DEBUG → INFO
