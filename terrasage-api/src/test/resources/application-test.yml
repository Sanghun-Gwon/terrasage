spring:
  sql:
    init:
      mode: never       # data.sql 비활성 — 테스트마다 직접 데이터 삽입
  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트 실행마다 스키마 재생성

encrypt:
  secret: test-encrypt-secret-key
  salt: "6163653536643234"
